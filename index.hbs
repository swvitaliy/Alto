{{!< default}}

<div class="content-area">
    <main class="site-main">
        <div class="post-feed">
            {{#get "posts" include="tags,authors" limit="10"}}
                {{#foreach posts}}
                    {{> "loop"}}
                {{/foreach}}
            {{/get}}
        </div>
        {{pagination}}
    </main>
</div>